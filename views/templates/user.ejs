<li class="list-group-item d-flex justify-content-between align-items-center">
    <b><%= user.name %></b> 
    (<%= user.user_type %>)
    <form action="/promote/<%= user.name%>" method="POST"><button class="badge bg-success">Promote to Admin</button></form>
    <form action="/demote/<%= user.name%>" method="POST"><button href="#" class="badge bg-danger">Demote to User</button></form>
</li>